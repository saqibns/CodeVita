#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    int tests, num, one, two;
    string result = "IsNotFibo";
    cin >> tests;
    while(tests--)
    {
        string result = "IsNotFibo";
        cin >> num;
        one = 5 * num * num + 4;
        two = 5 * num * num - 4;
        if(sqrt(one) == (int)
    }
